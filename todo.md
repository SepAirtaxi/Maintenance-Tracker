1: Please implement a feature that automatically declares an aircraft grounded if any scheduled event expires on date/hours, and maybe we should also introduce a toast function that advises the user (only users, not view-only) when an aircraft status has been changed to grounded due to expired event(s), and this notification should be acknowledged and dismissed to ensure it doesn't slip by.
2: In relation to the toast system om the previous todo request; we also need a toast/notification/warning that needs to be acknowledged when deferred defects needs to be extended or resolved after 30 days.
3: The mechanics have requested some sort of indicator for the resolution/deferral/NFF used to resolve events and defects, as when viewed in the calendar booking dialog. So right now they can see when an event/defect has been striked through, and they're requesting that the resolution is displayed in the pop-up dialogue so they know how it was resolved/deferred. If it was resolved normally on a WO, it should just state "resolved on WO xxxx", but if deferred or NFF, that also needs to be clearly stated. I am not sure if this will clash with legacy data though, so please think about my suggested logic and let me know if the current architecture will overwrite this. E.g. if a booking slot has been striked through as deferred, and then we book in the aircraft in the future for rectification of the deferral, I guess the legacy calendar bookings won't display this due to the WO-linked data being updated. Perhaps we can figure out a way to "hardcode" this specific historic data into specific calendar bookings, so we can still preserve the latest data for current/future booking, but also go back in time and see in the calendar when/why/how a booking was resolved. Need your input.