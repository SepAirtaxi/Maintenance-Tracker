1: Add TTAF value at resolutions for events, to be able to track when templated events were last carried out.
2: When creating a defect, please create a button to click that imports the current TTAF of the aircraft (defects are usually created the moment they are reported, and I can't read the aircraft TTAF when the creation dialog window is open).
3: Pertaining to the "Missing" events logic: I have just added a third 50-hour-anchored event that we need to include in the logic; "Annual Inspection". Can you please register this templated event as a 50 hour interval anchor as well, so it doens't flag aircraft with missing event if they have an Annual Inspection created already?
4: Can we introduce landings to the aircraft cards? It works the same as TTAF, so it's just a number of the all time accumulated landings. It's adjecent information in Flightlogger, so I don't know if you can implement a field for Landings on each aircraft, and with the current setup also pull this data via the API? The flightlogger api instructions can be found here: https://api.flightlogger.net/. I think the identifier is "totalLandings".