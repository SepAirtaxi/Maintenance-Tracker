1: Overview, visual correction: On defect lines, please move the "Status" column so it's aligned with the above "Status" column on the events.
2: Overview, new feature: We need to implement an option to extend scheduled events, and if an extionsion has been made should be clearly displayed. The CAMO has the option to extend (postpone TTAF due) up to 5 hours if necessary. So if flight ops has a requirement to use the aircraft a little before its next scheduled maintenance, I can grant an extension. This can only be done once per 50/100 hour interval, so please add a function to click "Extend", and enter the amount of hours extended (maximum 5). The due time should then clearly show that the displayed TTAF due time is including an extension of X hours.
3: Overview, new feature: For defects, we need an option to defer defects, which will then start a timer which runs for 30 days. It is company policy that we evaluate all deferred defects no later than every 30 days. Defects can be further deferred if necessary, but there must be an exclamation mark, notification or something else that clearly states when a defect has been deferred 30 days, and the CAMO then has to follow up.
4: Overview, new feature: Our planner has requested that we implement a function for their estimation data, which at this point only includes planned yes/no and estimated man hours.